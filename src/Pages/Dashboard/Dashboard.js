import React from 'react';
import useTitle from '../../Hooks/useTitle';

const Dashboard = () => {
    useTitle('Dashboard')
    return (
        <div>
            
        </div>
    );
};

export default Dashboard;